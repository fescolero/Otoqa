{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && if [ \"$VERCEL_ENV\" = \"production\" ]; then if [ -z \"$CONVEX_DEPLOY_KEY\" ]; then echo ''; echo '=============================================================='; echo 'PRODUCTION BUILD BLOCKED: CONVEX_DEPLOY_KEY is not set.'; echo ''; echo 'This Vercel project owns the production Convex deploy, so a'; echo 'build without the key would ship a console against a backend'; echo 'that was never deployed. Failing here is deliberate.'; echo ''; echo 'Fix (one-time, see apps/admin/README.md section 3b):'; echo '  1. Convex dashboard -> Settings -> Deploy Keys -> generate a'; echo '     PRODUCTION deploy key.'; echo '  2. Vercel -> this project -> Settings -> Environment Variables'; echo '     -> add CONVEX_DEPLOY_KEY, scoped to Production ONLY.'; echo '  3. Redeploy. The log should show convex deploy before next build.'; echo '=============================================================='; echo ''; exit 1; fi; bunx convex deploy --cmd 'cd apps/admin && bun run build' --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL; else cd apps/admin && bun run build; fi"
}
